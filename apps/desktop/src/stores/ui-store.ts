/**
 * UI Store for Desktop app
 *
 * Re-exports the shared UI store from @ordo-todo/stores
 * for cross-platform consistency.
 */

export { useUIStore, defaultUIState } from '@ordo-todo/stores';
export type { UIStore, UIStoreState } from '@ordo-todo/stores';
