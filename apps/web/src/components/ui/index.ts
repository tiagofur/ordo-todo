/**
 * UI Components - Re-exported from @ordo-todo/ui
 *
 * This file re-exports all UI components from the shared package
 * to maintain backward compatibility with existing imports.
 *
 * Usage: import { Button } from '@/components/ui/button'
 * or:    import { Button } from '@ordo-todo/ui'
 */

// Re-export all components from the shared package
export * from "@ordo-todo/ui";

// Note: Components are now maintained in packages/ui
// Any local modifications should be made there
