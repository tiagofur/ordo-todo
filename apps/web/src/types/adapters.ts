// Adapters for converting between API DTOs and UI component interfaces
// This file acts as a compatibility layer ensuring strict type safety
// when API responses do not exactly match UI component expectations.

import type { TaskCardTask } from "@ordo-todo/ui";
// import type { Task } from "@ordo-todo/api-client";

// Example adapter (placeholder until specific mapping is needed)
// export const apiTaskToCardTask = (apiTask: Task): TaskCardTask => {
//   return {
//     id: apiTask.id,
//     title: apiTask.title,
//     // ... other mappings
//   };
// };
