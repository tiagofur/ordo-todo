/**
 * Timer Hook for Web App
 *
 * Re-exports the shared useTimer hook from @ordo-todo/hooks
 * for cross-platform consistency.
 */

export {
  useTimer,
  type TimerMode,
  type TimerType,
  type TimerConfig,
  type UseTimerProps,
  type SessionData,
  type UseTimerReturn,
} from '@ordo-todo/hooks';
