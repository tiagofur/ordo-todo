import { View } from 'react-native';

// This is a dummy component just to satisfy the Router requiring a component for the tab.
// The navigation is handled by the tabBarButton in _layout.tsx
export default function ProjectsNavPlaceholder() {
  return <View />;
}
