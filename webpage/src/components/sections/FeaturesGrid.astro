---
import { getLangFromUrl, useTranslations } from '../../i18n/utils';
import { getCollection } from 'astro:content';
import Card from '../ui/Card.astro';

const currentLang = getLangFromUrl(Astro.url);
const t = useTranslations(currentLang);

const features = await getCollection('features');

const featureCategories = [
  { key: 'ai', icon: '🤖' },
  { key: 'productivity', icon: '⚡' },
  { key: 'collaboration', icon: '👥' },
  { key: 'time-management', icon: '⏰' }
];
---

<section class="py-20 bg-white dark:bg-gray-900">
  <div class="container mx-auto px-4 sm:px-6 lg:px-8">
    <!-- Section Header -->
    <div class="text-center mb-16">
      <h2 class="text-3xl sm:text-4xl font-bold text-gray-900 dark:text-white mb-4">
        Everything You Need to Stay Productive
      </h2>
      <p class="text-xl text-gray-600 dark:text-gray-300 max-w-3xl mx-auto">
        Powerful features designed to help you achieve more while maintaining work-life balance
      </p>
    </div>

    <!-- Features Grid -->
    <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-4 gap-8">
      {featureCategories.map(category => {
        const categoryFeatures = features.filter(f => f.data.category === category.key);
        
        return (
          <div class="space-y-6">
            <div class="flex items-center space-x-3">
              <span class="text-3xl">{category.icon}</span>
              <h3 class="text-xl font-semibold text-gray-900 dark:text-white">
                {t(`features.${category.key}.title`)}
              </h3>
            </div>
            
            <div class="space-y-4">
              {categoryFeatures.map(feature => (
                <Card variant="compact" hover={true}>
                  <div class="flex items-start space-x-3">
                    <div class="flex-shrink-0 w-10 h-10 bg-blue-100 dark:bg-blue-900 rounded-lg flex items-center justify-center">
                      <span class="text-lg">{feature.data.icon}</span>
                    </div>
                    <div>
                      <h4 class="font-medium text-gray-900 dark:text-white mb-1">
                        {feature.data.title}
                      </h4>
                      <p class="text-sm text-gray-600 dark:text-gray-400">
                        {feature.data.description}
                      </p>
                    </div>
                  </div>
                </Card>
              ))}
            </div>
          </div>
        );
      })}
    </div>

    <!-- Featured AI Features -->
    <div class="mt-16 bg-gradient-to-r from-blue-50 to-purple-50 dark:from-blue-900/20 dark:to-purple-900/20 rounded-2xl p-8">
      <div class="text-center mb-8">
        <h3 class="text-2xl font-bold text-gray-900 dark:text-white mb-2">
          🚀 New AI Features
        </h3>
        <p class="text-gray-600 dark:text-gray-300">
          Experience the future of productivity with our latest AI-powered capabilities
        </p>
      </div>
      
      <div class="grid grid-cols-1 md:grid-cols-3 gap-6">
        <Card hover={true} class="text-center">
          <div class="text-4xl mb-4">📝</div>
          <h4 class="font-semibold text-gray-900 dark:text-white mb-2">AI Meeting Summarizer</h4>
          <p class="text-sm text-gray-600 dark:text-gray-400">
            Automatically join video calls, generate action items, and assign tasks to attendees
          </p>
        </Card>
        
        <Card hover={true} class="text-center">
          <div class="text-4xl mb-4">📧</div>
          <h4 class="font-semibold text-gray-900 dark:text-white mb-2">Smart Email Integration</h4>
          <p class="text-sm text-gray-600 dark:text-gray-400">
            Convert emails to tasks with one click, detect priorities automatically
          </p>
        </Card>
        
        <Card hover={true} class="text-center">
          <div class="text-4xl mb-4">🔮</div>
          <h4 class="font-semibold text-gray-900 dark:text-white mb-2">Predictive Analytics</h4>
          <p class="text-sm text-gray-600 dark:text-gray-400">
            ML-based duration prediction and optimal timing suggestions
          </p>
        </Card>
      </div>
    </div>
  </div>
</section>